allow vendor_init rootfs:dir { w_dir_perms add_name };
allow vendor_init sysfs_boot_adsp:file { create write };

allow vendor_init device:file create;

allow vendor_init proc_touchpanel:file { read setattr };
